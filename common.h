#ifndef clox_common_h
#define clox_common_h

//There are a handful of types and constants we’ll use throughout the interpreter,
//and this is a convenient place to put them. For now, it’s the venerable NULL and
//size_t along with the nice C99 Boolean and explicit-sized integer types—bool, uint8_t,
//and friends.
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#endif